{
    "name": "timecapsule-frontend",
    "version": "1.0.0",
    "description": "Frontend for TimeCapsule application",
    "main": "index.js",
    "scripts": {
      "start": "live-server src --port=8080 --host=0.0.0.0 --no-browser",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
      "live-server": "^1.2.2"
    }
  }